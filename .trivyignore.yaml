vulnerabilities:
  # Transitive Prisma CLI deps -- not used in runtime, no fix available
  - id: CVE-2026-29087
  - id: CVE-2026-29045

misconfigurations:
  # Dev SSH targets intentionally run sshd with privileged defaults for local acceptance testing.
  - id: DS-0002
    paths:
      - infrastructure/dev/debian-ssh-target/Dockerfile
      - infrastructure/dev/terminal-target/Dockerfile
  - id: DS-0004
    paths:
      - infrastructure/dev/debian-ssh-target/Dockerfile
  - id: DS-0026
    paths:
      - infrastructure/dev/debian-ssh-target/Dockerfile
      - infrastructure/dev/terminal-target/Dockerfile

secrets:
  # .env.example contains placeholder values, not real secrets
  - path: .env.example
  # Ansible dev compose template contains Jinja2 placeholders, not real secrets
  - path: deployment/ansible/roles/deploy/templates/compose.dev.yml.j2
